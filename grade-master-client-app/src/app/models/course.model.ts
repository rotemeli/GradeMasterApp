export interface Course {
  id: string;
  teacherId: string;
  courseName: string;
  description: string;
  numberOfLectures: string;
}

export interface Mock {
  id: string;
  title: string;
  description: string;
}
